<p style="color: green"><%= notice %></p>

<h1>Memos</h1>

<div data-controller="btn">
  <div class="btn btn-primary m-5"       data-btn-target="btn" data-action="click->btn#change_color">
  ボタン
  </div>
</div>


<div id="memos">
  <% @memos.each do |memo| %>
    <%= render memo %>
    <p>
      <%= link_to "Show this memo", memo %>
    </p>
  <% end %>
</div>

<%= link_to "New memo", new_memo_path %>
